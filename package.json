{
  "devDependencies": {
    "parcel": "^1.12.4",
    "purescript": "^0.13.8",
    "spago": "^0.16.0"
  },
  "private": true,
  "scripts": {
    "build": "spago build",
    "build-prod": "mkdir -p public/prod && cp public/dev/index.html public/prod/ && spago bundle-app --to public/prod/index.js && parcel build public/prod/index.html",
    "serve": "parcel public/dev/index.html --open",
    "test": "spago test"
  }
}
